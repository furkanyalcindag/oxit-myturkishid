class ProfileControlObject:

    def __init__(self, profile, is_controlled, total_order):
        self.id = id
        self.profile = profile
        self.is_controlled = is_controlled
        self.total_order = total_order
